<Window x:Class="PipeConnection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PipeConnection"
        mc:Ignorable="d"
        Title="Pipe Server Manager" Height="450" Width="600"
        Loaded="Window_Loaded" Unloaded="Window_Unloaded"
        >
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="StartServerButton" Content="Start Server" 
                    Click="StartServer_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button x:Name="StopServerButton" Content="Stop Server" 
                    Click="StopServer_Click" IsEnabled="False" Margin="0,0,10,0" Padding="10,5"/>
            <Button x:Name="ClearLogButton" Content="Clear Log" 
                    Click="ClearLog_Click" Margin="0,0,10,0" Padding="10,5"/>
        </StackPanel>

        <Label x:Name="StatusLabel" Grid.Row="1" Content="Server not started" 
               Margin="0,0,0,10" FontWeight="Bold"/>

        <TextBox x:Name="LogTextBox" Grid.Row="2" 
                 IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 Background="Black"
                 Foreground="White"
                 Margin="0,0,0,10"/>

        
    </Grid>
</Window>

